import { View } from 'react-native';

export default function Index() {
  // Routing is handled in _layout.tsx based on AsyncStorage
  return <View style={{ flex: 1, backgroundColor: '#1C1C1E' }} />;
}
